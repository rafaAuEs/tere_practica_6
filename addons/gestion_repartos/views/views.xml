<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="view_reparto_vehiculo_tree" model="ir.ui.view">
            <field name="name">reparto.vehiculo.tree</field>
            <field name="model">reparto.vehiculo</field>
            <field name="arch" type="xml">
                <tree>
                    <field name="tipo"/>
                    <field name="matricula"/>
                    <field name="estado"/>
                </tree>
            </field>
        </record>

        <record id="action_reparto_vehiculo" model="ir.actions.act_window">
            <field name="name">Vehículos</field>
            <field name="res_model">reparto.vehiculo</field>
            <field name="view_mode">tree,form</field>
        </record>

        <record id="action_reparto_cliente" model="ir.actions.act_window">
            <field name="name">Clientes</field>
            <field name="res_model">reparto.cliente</field>
            <field name="view_mode">tree,form</field>
        </record>

        <record id="action_reparto_empleado" model="ir.actions.act_window">
            <field name="name">Empleados</field>
            <field name="res_model">reparto.empleado</field>
            <field name="view_mode">tree,form</field>
        </record>

        <record id="action_reparto_pedido" model="ir.actions.act_window">
            <field name="name">Repartos</field>
            <field name="res_model">reparto.pedido</field>
            <field name="view_mode">tree,form</field>
        </record>

        <menuitem id="menu_reparto_root" name="Gestión Repartos" sequence="10"/>

        <menuitem id="menu_reparto_operaciones" name="Operaciones" parent="menu_reparto_root" sequence="10"/>
        <menuitem id="menu_reparto_pedidos" name="Pedidos de Reparto" parent="menu_reparto_operaciones" action="action_reparto_pedido" sequence="10"/>

        <menuitem id="menu_reparto_config" name="Datos Maestros" parent="menu_reparto_root" sequence="20"/>
        <menuitem id="menu_reparto_empleados" name="Empleados" parent="menu_reparto_config" action="action_reparto_empleado" sequence="10"/>
        <menuitem id="menu_reparto_vehiculos" name="Vehículos" parent="menu_reparto_config" action="action_reparto_vehiculo" sequence="20"/>
        <menuitem id="menu_reparto_clientes" name="Clientes" parent="menu_reparto_config" action="action_reparto_cliente" sequence="30"/>
    </data>
</odoo>